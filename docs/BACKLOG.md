# 남은 작업

지금 열려 있는 작업 목록. **배경 설명은 여기 복붙하지 않고 원문을 가리킨다** — 상세는
`UI-SYSTEM.md`의 "남은 것", `CONVENTIONS.md`·`ERD.md`의 "알려진 한계", `PRODUCT.md`의
"미정 사항"이 각각 진실 소스다. 여기는 "무엇이 열려 있고 언제 하면 되는지"만 관리한다.

기준 커밋: `c1d424c` (ERD 문서화 + GPS 노출 범위 정정). 열린 GitHub 이슈 없음.

---

## 1. 바로 할 수 있는 것

막힌 것 없이 지금 착수 가능한 것들. 위에서부터 값어치 순.

### 1-1. lint 부채 정리 — 별도 커밋

`npm run lint`가 레포 전체에서 실패한다 — 128건(2026-09-02 측정). 이번 작업들과 무관한 기존 부채다.

- **78건은 `npm run lint -- --fix` 한 방**으로 끝난다(71건이 prettier 줄바꿈).
- 나머지 50건은 손으로 — 43건이 `no-restricted-syntax`(함수 선언식 → 화살표 함수),
  나머지는 `naming-convention`(불리언 변수 접두사)·`react-hooks/set-state-in-effect`·`max-params`.
- 특정 디렉터리에 몰려 있지 않다 — 레포 전체다. 스코프를 미리 좁히지 말 것.

**CI가 없어서 이게 계속 쌓인다.** 정리 커밋과 함께 pre-commit 훅이나 CI를 붙일지 판단이 필요하다
— 안 붙이면 다음 세션에 또 같은 양이 쌓인다.

### 1-2. 터치 타겟 44px 마무리 (a11y)

`EntryDetailModal`의 "삭제하기"부터 — 셋 중 유일하게 WCAG 최소 크기에도 미달한다.
→ `UI-SYSTEM.md` "남은 것" (어디가 몇 px인지 거기 있다)

### 1-3. `role="status"` vs `role="alert"` 기준 통일

화면마다 섞여 있고 role이 아예 없는 곳도 있다. → `UI-SYSTEM.md` "남은 것" (어디가 어떤지 거기 있다)

---

## 2. 트리거가 오면 반드시 — 조건부

지금 고치면 과설계지만, 아래 조건이 오는 순간 **같이** 해야 하는 것들.
조건 없이 미리 하지 말 것.

### 2-1. `photo_path`의 user_id 노출 → **프로필·팔로우가 붙을 때**

프로필이 붙는 순간 user_id로 사람을 특정할 수 있게 되고, 그때 `photo_path`가 그룹핑을
그대로 넘겨준다. **경로만 먼저 바꾸면 storage 쓰기 권한이 열린다** — 반드시 함께 움직일 것.
→ `CONVENTIONS.md` "알려진 한계" (배경·영향 범위·함께 고칠 지점 전부 거기 있다)

### 2-2. `PlaceholderPhoto`의 `isLoaded` 리셋 → **서버 프리페치를 도입할 때**

지금은 드러나지 않을 뿐이다 — 서버 프리페치를 붙이는 순간 사진이 영영 안 보이게 된다.
프리페치 작업과 **같은 커밋에서** 함께 고쳐야 한다. → `UI-SYSTEM.md` "남은 것" (증상·원인 거기 있다)

### 2-3. 사진첩 조회를 서버 필터로 → **로더 대기가 눈에 띄게 길어지면**

`CalendarView`가 전체 기록을 받아 `isMine`으로 걸러 대부분 버린다(→ `FLOWS.md` §3).
지금 규모에선 조회 훅을 피드와 공유하는 게 맞다. 옮길 땐 `entry_feed`가 뷰라
`.eq("is_mine", true)`가 PostgREST에서 그대로 먹고, 그때 클라이언트 필터 한 줄도 같이 지워진다.

### 2-4. `useCloudEntries`의 최초 조회 토큰 누락 → **빈 사진첩 제보가 들어오면**

`8671f4e`로 `onAuthStateChange` 구독이 붙어 **로그아웃 시 재조회는 해결됐다.** 남은 구멍은
최초 조회다 — `is_mine`은 fetch 시점 토큰으로 서버가 계산하는데, 그 요청에 토큰이 안 실리면
**에러 없이 조용히 빈 사진첩**이 된다(로그가 안 남아 재현이 어렵다). 첫 이벤트
(`INITIAL_SESSION`)는 최초 조회와 겹친다고 건너뛰므로 자동 복구 지점이 없고, 실제
로그아웃→로그인으로 user id가 바뀌기 전까지 그대로다.
코드상 깨질 확률은 낮다 — `auth/callback`이 서버에서 쿠키를 굽고 풀 리다이렉트를 한다.

### 2-5. `cloud_entries`의 원본 좌표 노출 → **`lat`/`lng`가 실제로 쌓인 행이 생기는 순간**

지금은 저장된 좌표가 전부 `null`이라 안 급하지만, `POST /api/entries/confirm`이 좌표를
insert하므로 **행이 하나만 쌓여도 "정확한 좌표를 노출하지 않는다"는 확정 스펙이 깨진다.**
2-1과 같은 뿌리다(뷰만 좁히고 테이블은 안 좁혔다).

**select 정책을 좁히는 방법은 쓸 수 없다 — 피드가 같이 죽는다.** 실제로 가능한 선택지
두 개와 그 근거는 → `ERD.md` "알려진 한계" › "고칠 때 — select 정책을 좁히는 건 답이 아니다"

---

## 3. 결정이 필요한 것 — 사람이 정해야 진행됨

### 3-1. 서비스명 (다른 것들을 막고 있다)

공유 카드 워터마크가 이름을 기다리며 플레이스홀더 문자열 그대로 나가고 있다 —
사람이 이름을 정해야 진행된다. 후보("뭉게뭉게")와 막혀 있는 지점은
→ `PRODUCT.md` "미정 사항"

### 3-2. IA 중복 — 사진첩이 같은 데이터를 두 번 보여준다

화면 구조 재설계라 `PRODUCT.md` 플로우와 함께 다뤄야 한다 — 사람이 IA를 정해야 진행된다.
→ `UI-SYSTEM.md` "남은 것"

### 3-3. 모달 exit 애니메이션 — 할지 말지

고치려면 Dialog root를 상시 마운트하고 `open`만 토글하는 구조 변경이 필요하다 —
호출부 전체가 바뀌므로 값어치를 먼저 판단할 것. → `UI-SYSTEM.md` "남은 것" (원인 거기 있다)

---

## 4. 확인만 하면 되는 것

- [ ] **카카오 로그인 → 사진첩에서 내 기록이 실제로 보이는지 브라우저로 한 번.**
      `ffbd26f`에서 사진첩이 `is_mine`에 전적으로 의존하게 됐는데, 실패하면
      에러 없이 빈 화면이라 나중에 재현이 어렵다(2-4 참고).
