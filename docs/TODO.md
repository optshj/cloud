# TODO — 남은 작업

## 2. 트리거가 오면 반드시 — 조건부

지금 고치면 과설계지만, 아래 조건이 오는 순간 **같이** 해야 하는 것들.
조건 없이 미리 하지 말 것.

### 2-1. `entry_likes`가 user_id를 그대로 내보낸다 → **프로필·팔로우가 붙을 때**

(2026-09-08 확인 — `photo_path`/`cloud_entries.user_id` 노출을 `0004`로 막으면서 같은 뿌리로 남은 것)

`entry_likes`의 select 정책이 `using (true)`고 컬럼 권한도 그대로라 anon 키로
`GET /rest/v1/entry_likes?select=entry_id,user_id`가 응답한다 — "이 좋아요들이 같은 사람 것"이라는
그룹핑이 나오고, 자기 글에 좋아요를 누른 사람은 기록 하나가 특정된다.

사진 쪽(`0004`)만큼 직접적이지 않고 프로필이 없어 user_id로 사람을 특정할 수단이 아직 없어서 v1에선
알려진 한계로 둔다. **프로필이 붙는 순간 같이 막는다.** 고칠 땐 `0004`와 같은 모양이 될 것이다 —
`fetchEntries`가 `.eq("user_id", ...)`로 필터하니 그 컬럼 권한을 회수하려면 "내가 좋아요 했는지"를
서버에서 계산해 내려주는 쪽으로 옮겨야 한다(뷰 컬럼 또는 `security definer` 함수).
→ `src/entities/cloud-entry/model/api.ts`의 `fetchEntries`, `docs/ERD.md` "알려진 한계"

### 2-2. `PlaceholderPhoto`의 `isLoaded` 리셋 → **서버 프리페치를 도입할 때**

`isLoaded`가 `photoDataUrl` 변경 시 리셋되지 않는다. 지금은 목록이 빈 배열로 시작해 SSR HTML에
`<img>`가 안 실리므로 드러나지 않는다. 서버에서 entries를 프리페치하는 순간 캐시된 이미지의
`onLoad`가 유실돼 **사진이 `opacity-0`으로 영영 안 보일 수 있다.** 프리페치 작업과 **같은 커밋에서**
함께 고쳐야 한다.
→ `src/shared/ui/PlaceholderPhoto.tsx`

### 2-3. 사진첩 조회를 서버 필터로 → **로더 대기가 눈에 띄게 길어지면**

`CalendarView`가 전체 기록을 받아 `isMine`으로 걸러 대부분 버린다(→ `FLOWS.md` §3).
지금 규모에선 조회 훅을 피드와 공유하는 게 맞다. 옮길 땐 `entry_feed`가 뷰라
`.eq("is_mine", true)`가 PostgREST에서 그대로 먹고, 그때 클라이언트 필터 한 줄도 같이 지워진다.
→ `src/views/calendar/ui/CalendarView.tsx`의 `allEntries.filter((e) => e.isMine)`

### 2-4. `useCloudEntries`의 최초 조회 토큰 누락 → **빈 사진첩 제보가 들어오면**

`8671f4e`로 `onAuthStateChange` 구독이 붙어 **로그아웃 시 재조회는 해결됐다.** 남은 구멍은
최초 조회다 — `is_mine`은 fetch 시점 토큰으로 서버가 계산하는데, 그 요청에 토큰이 안 실리면
**에러 없이 조용히 빈 사진첩**이 된다(로그가 안 남아 재현이 어렵다). 첫 이벤트
(`INITIAL_SESSION`)는 최초 조회와 겹친다고 건너뛰므로 자동 복구 지점이 없고, 실제
로그아웃→로그인으로 user id가 바뀌기 전까지 그대로다.
코드상 깨질 확률은 낮다 — `auth/callback`이 서버에서 쿠키를 굽고 풀 리다이렉트를 한다.
→ `src/entities/cloud-entry/model/use-cloud-entries.ts`

### 2-6. 셔터의 "위치 확인 중..." 배지 되살리기 → **fix가 정말 차가울 때가 문제가 되면**

진행 배지는 없다. 재방문자가 물고 있던 대기는 미리 받기로 없앴다 — 경위와 현재 동작은
→ `UI-SYSTEM.md` "결정" §8, 회귀는 `e2e/smoke.spec.ts`의 "셔터를 누르기 전에 위치를 미리 받아둔다"가 막는다.

**남은 건 fix 자체가 차가운 경우다.** 실내·GPS 콜드 스타트라 미리 받기도 8초를 다 쓰면 셔터도
같이 기다린다. 이건 캐시로 못 줄이니 그때는 배지를 되살리는 게 맞다.

### 2-9. `PageTransition`의 준비 신호가 트랜지션별로 구분 안 됨 → **View가 로딩을 재발화할 때**

(2026-09-07 확인, 리뷰에서 발견)

탭 전환 오버레이는 목적지 View가 `usePageReady(!isLoading)`로 "준비됨"을 알릴 때까지 화면을
덮은 채 기다린다(`PageTransition.tsx`). 이 신호는 `isPageReadyRef`/`onPageReadyRef` 두
module 스코프 ref로 관리되는데, **어느 트랜지션의 신호인지 구분하지 않는다** — 새 트랜지션
시작 시 리셋만 할 뿐, 이미 마운트된 이전 화면이 나중에 다시 `isReady`를 뒤집으면(예: 포커스
복귀 시 refetch로 `isLoading`이 true→false로 재발화) 그 신호가 엉뚱하게 새 트랜지션의 대기를
조기 종료시킬 수 있다.

`useCloudEntries`/`useSession`이 지금은 `isLoading`을 마운트당 한 번만 false로 떨어뜨리는
단조(monotonic) 값이라 **오늘은 재현되지 않는다** — View 구현이 우연히 지켜주는 불변식이지
타입으로 강제된 게 아니다. **어떤 View가 `usePageReady`에 refetch·포커스 재검증처럼 뒤늦게
다시 뒤집힐 수 있는 조건을 넣는 순간** 조용히 깨진다. 그때 트랜지션마다 세대(generation)
토큰을 붙여 `setPageReady`가 "지금 활성 트랜지션의 신호인지"를 확인하도록 고친다.
→ `src/widgets/app-shell/ui/PageTransition.tsx`

### 2-10. `POST /api/entries/confirm`이 `photoPath`를 검증하지 않는다 → **남의 사진으로 만든 기록이 실제로 보이면**

(2026-09-08 확인 — `0004` 작업 중 §2-1에서 떼어낸 것. 경로 컨벤션과 무관하게 예전부터 열려 있었다)

confirm은 클라이언트가 보낸 `photoPath` 문자열을 검증 없이 그대로 insert한다. 경로는 피드에
그대로 실리는 공개값이라, 남의 사진 경로를 넣어 **자기 기록으로 올릴 수 있다.** 버킷이 public이라
읽기는 어차피 열려 있으니 새로 새는 정보는 없고, 남는 건 "내 사진이 남의 기록으로 보인다"는
표시상의 문제다. 막으려면 업로더(`owner_id`)가 요청자인지 확인해야 하는데 storage 스키마가
PostgREST에 없어 `0004`의 `entry_photo_paths`처럼 함수를 하나 더 열어야 한다 — 지금 비용이 더 크다.

**`POST /api/entries/preview`도 같은 구멍이다**(2026-09-09 확인) — `photoPath`를 검증 없이
`getPublicUrl`에 넣는다. 실질 영향은 "남의 사진으로 AI 코멘트를 받아본다" 정도로 더 얕지만,
막을 땐 두 라우트를 같이 고쳐야 한다.
→ `app/api/entries/confirm/route.ts`, `app/api/entries/preview/route.ts`

### 2-8. e2e 확장 2건 → **각각 세션 주입/모킹 방법이 정해지면**

- **로그인 세션 주입 방식이 정해지면** 촬영 → 기록 플로우 자체를 e2e로 확장한다. 지금은 실제
  카카오 인증이 필요해 자동화 밖이다. 로그인 없이 갈 수 있는 범위(탭 이동 등)는 `e2e/smoke.spec.ts`가
  이미 커버 중이다.
- **`getUserMedia` 모킹 방법이 정해지면** "익명 촬영 → 사진만 보이는 미리보기 → 로그인 유도 버튼
  노출"까지 e2e로 확장한다.

### 2-11. confirm이 받은 `tag`/`comment`가 preview 결과와 같은지 확인하지 않는다 → **남의 문구가 실제로 문제되면**

(2026-09-09 확인 — 전체 리뷰)

confirm은 클라이언트가 보낸 `tag`/`comment`를 그대로 저장한다. AI가 만든 값이라는 보장이 없어서
아무 문자열이나 "AI 코멘트"로 공개 피드에 올릴 수 있다. **길이 상한(태그 20자·코멘트 100자)은
2026-09-09에 서버에 넣었으니** 무제한 텍스트가 DB에 들어가는 문제는 막혔고, 남은 건 내용 대조다.
고치려면 preview 결과를 짧은 TTL로 서버가 들고 있다가 confirm에서 맞춰봐야 하는데(저장소가 하나
더 생긴다), 지금 스코프에서 그 비용이 더 크다. confirm에서 AI를 다시 부르는 건 비용 2배라 제외.
→ `app/api/entries/confirm/route.ts`

### 2-12. 달력 그리드에 방향키 이동이 없다 → **키보드/스크린리더 사용자 제보가 오면, 또는 a11y를 한 번 훑을 때**

(2026-09-09 확인 — 전체 리뷰)

`accessibility` 스킬이 `MonthCalendarGrid`에 요구한 두 가지 중 `aria-current`만 돼 있다. 기록이
있는 날만 `<button>`이라 Tab 순서가 달력 모양과 무관하게 흩어진다. roving tabindex + 방향키가
정석인데 작업량이 있어 지금은 열어둔다.
→ `src/widgets/month-calendar/ui/MonthCalendarGrid.tsx`

### 2-13. `downloadDataUrl`의 `<a download>` → **웹뷰로 감쌀 때**

(2026-09-09 확인 — 전체 리뷰)

`<a download>`는 안드로이드 WebView/iOS WKWebView에서 아무 동작도 하지 않는 경우가 많다. 웹 우선
정책상 지금은 문제가 없지만, 웹뷰 앱화(→ `CLAUDE.md` 기술 스택)를 시작하면 공유 카드 저장이
조용히 죽는다. 그때 네이티브 브리지나 `share()` API로 갈아탄다.
→ `src/features/share-card/lib/share-card.ts`

---

## 3. 결정이 필요한 것 — 사람이 정해야 진행됨

### 3-1. 서비스명 (다른 것들을 막고 있다)

아직 결정되지 않았다. 임의로 확정 짓지 말고 사용자에게 확인한다.

후보가 하나 있다 — Supabase 프로젝트명이 **"뭉게뭉게"**로 돼 있다. 이걸 그대로 확정할지 따로
정할지가 남은 결정이다.

이름이 없어서 **공유 카드 워터마크가 플레이스홀더 문자열 그대로 나가고 있다**
(`src/features/share-card/lib/share-card.ts`의 `ctx.fillText("서비스 로고 · 나도 오늘 구름 기록하기", ...)`).
카드 이미지 내보내기는 바이럴 도달용 기능이라(→ `PRODUCT.md` "확정된 제품 스펙" 외부 공유)
이름이 정해지기 전까지 그 목적을 못 한다.

### 3-2. 클라이언트 조회 계층을 어느 문서가 관할하는지

(2026-09-02 확인)

`useCloudEntries` 같은 클라이언트 조회 계층의 **동작·실패 모드**를 소유하는 문서가 없다.
위 2-3·2-4가 그 예다. 후보를 다 짚어봤지만 전부 관할 밖이었다 — `UI-SYSTEM.md`는 프리미티브·상태
표현·모션, `FLOWS.md`는 "지금 동작하는" 화면 순서(잠재 실패 모드가 아니다), `ERD.md`는 스키마다.

관할이 겹칠 때가 아니라 **비었을 때**라 임의로 새 문서를 만들면 그게 다음 드리프트 면이 된다.
같은 종류(훅·캐시·재조회) 항목이 또 나오면 이 건과 묶어서 사용자에게 묻는다.

### 3-3. 탈퇴 시 사진 삭제가 실패하면 중단할까, 계속 진행할까

(2026-09-09 확인 — 전체 리뷰)

`DELETE /api/account`는 *사진 경로 조회* 실패엔 "사진만 남으면 되돌릴 수 없다"며 중단하는데,
정작 *삭제* 실패엔 그대로 `deleteUser`로 넘어간다 — 같은 이유에 앞뒤가 안 맞는다. 계정이 사라지면
경로 목록을 재구성할 수 없어 public 버킷에 사진이 영구히 남는다. 실패 로그는 2026-09-09에 붙였다.
정할 것: **(a)** 삭제 실패도 500으로 중단(계정은 남고 사용자가 재시도) · **(b)** 현행 유지 + 로그만.
"탈퇴 시 즉시 전체 삭제"(→ `PRODUCT.md`)를 지키는 건 (a)지만, 스토리지 장애에 탈퇴가 막힌다.
→ `app/api/account/route.ts`

### 3-4. `Button` 프리미티브의 최소 높이를 44px로 올릴지

(2026-09-09 확인 — 전체 리뷰)

`buttonVariants`의 `default`는 36px, `sm`은 28px이다. 지금은 터치 타겟이 중요한 곳마다 호출부에서
`min-h-11`을 붙이고 있는데(카메라 확인 화면, 권한 게이트, 카메라 에러, 사진첩 삭제), 새 버튼을
만들 때마다 같은 실수가 반복된다. 프리미티브에서 올리면 앱 전체 버튼이 8px씩 커진다 — 디자인
결정이라 임의로 못 바꾼다. 정할 것: **(a)** 프리미티브에서 일괄 44px · **(b)** 지금처럼 호출부에서
개별 지정.
→ `src/shared/ui/button.tsx`

### 3-5. 태그를 피드 밖에서도 보여줄지

(2026-09-09 확인 — 전체 리뷰)

`tag`는 지금 **피드 격자와 피드 상세에서만** 보인다. 사진첩 상세에도, 촬영 직후 미리보기에도 없어서
**자기 기록의 태그를 저장 전에 한 번도 못 본다.** 같은 기록이 보는 자리마다 다르게 읽힌다.
정할 것: **(a)** 사진첩 상세·미리보기에도 태그를 넣어 통일 · **(b)** 태그는 피드 전용 정보로 둔다.
→ `src/features/capture-cloud/ui/CapturePreview.tsx`, `src/widgets/month-calendar/ui/EntryDetailModal.tsx`

### 3-6. `entry-photos` 버킷에 MIME/용량 제한을 걸지

(2026-09-09 확인 — 전체 리뷰)

`0004`의 insert 정책은 `bucket_id`만 본다(소유권 근거가 없어서 — 그 판단 자체는 타당). 남는 건
로그인 사용자가 임의 MIME·임의 크기 파일을 public 버킷에 올릴 수 있다는 것. Supabase 대시보드
설정이라 마이그레이션에 안 남아서 **사람이 콘솔에서 걸어야 한다**(Allowed MIME types `image/jpeg`,
file size limit). 걸면 `ERD.md` "Storage"에 한 줄 남긴다.

---

## 4. 확인만 하면 되는 것

- [ ] **카카오 로그인 → 사진첩에서 내 기록이 실제로 보이는지 브라우저로 한 번.**
      `ffbd26f`에서 사진첩이 `is_mine`에 전적으로 의존하게 됐는데, 실패하면
      에러 없이 빈 화면이라 나중에 재현이 어렵다(2-4 참고).
- [ ] **하드웨어 줌이 실기기(안드로이드 크롬)에서 실제로 당겨지는지.** capability 유무로 갈리는
      코드라 데스크톱 웹캠에선 디지털 줌 경로만 타서 확인이 안 된다. 볼 것: 슬라이더를 밀 때
      뷰파인더가 당겨지는지, 배지 배율이 1x부터 시작하는지, 촬영 결과가 뷰파인더와 같은
      화각인지, **슬라이더를 최소로 되돌리면 광각으로 돌아오는지**(중간 배율에서 거절당하면
      센서는 당겨진 채 남고 배지만 1.0x로 내려올 수 있다). 거절되면 콘솔에 "하드웨어 줌 적용
      실패, 디지털 줌으로 전환"이 남는다. (iOS Safari는 지원 자체가 없어 디지털 줌이 정상이다.)
      **이때 `getCapabilities().zoom` 실제 값을 콘솔로 찍어둘 것** — 기기가 광고하는 범위·step을
      봐야 키보드 조작(범위가 넓으면 화살표 키로 수백 번 눌러야 한다) 대응을 정할 수 있다.
