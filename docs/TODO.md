# TODO

실기기 사용 중 발견한 문제를 모아두던 자리. **네 건 전부 구현 완료라 여기 열린 항목은 없다**
(`44ea260` 카메라 3종 + `5770ae3` 하단 네비 + `83cca85` 오버레이·줌 2단계·`maximumAge`).

이 문서의 관할("무엇을 해야 하는가")은 `BACKLOG.md`가 선언한 관할과 그대로 겹친다 — 새로 열리는
작업은 `BACKLOG.md`에 적는다. 완료된 네 건의 **결정 기록**은 각 관할 문서로 옮겼다:

| 항목 | 이관처 |
| --- | --- |
| 1. 권한 요청 타이밍 — 진입 게이트 | → [`FLOWS.md`](FLOWS.md) §1 (플로우 단계 + "방문 중에만 허용" 한계) |
| 2. 촬영 후 미리보기 — 시각적 오버레이 | → [`UI-SYSTEM.md`](UI-SYSTEM.md) "결정" §8 (X버튼 규격 통일 포함) |
| 3. 좌우 border — 전역 삭제 | → [`UI-SYSTEM.md`](UI-SYSTEM.md) "결정" §8 |
| 4. 줌 슬라이더 | → [`UI-SYSTEM.md`](UI-SYSTEM.md) "결정" §8 (배지 ↔ 슬라이더 2단계 + 셔터 진행 배지 제거) |

이 파일은 지워도 된다 — 남겨둔 건 이관 흔적을 사용자가 한 번 확인하라고 둔 것뿐이다.
